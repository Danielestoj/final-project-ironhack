providers = ["python"]

[phases.setup]
nixPkgs = ["python3", "python311Packages.pip", "gfortran", "openblas"]

[phases.install]
cmds = ["pip install --no-cache-dir --break-system-packages -r requirements.txt"]
